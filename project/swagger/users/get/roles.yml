Listado de roles
  ---
  definitions:
      UsuarioDTO:
          type: object
          properties:
              roles:
                  type: array
                  items: string
  responses:
      200:
          description: Listado de roles
          schema:
              $ref: '#/definitions/UsuarioDTO'