Listado de roles
---
tags:
  - Usuario
components:
  schemas:
    RolesDTO:
      type: object
      properties:
        roles:
          type: array
          items:
            description: rol
            type: string
responses:
  200:
    description: Listado de roles
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/RolesDTO'
